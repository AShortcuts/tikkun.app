import type { RangeAudioRecording } from '../audio/types.ts'

// Reusable holiday recordings are keyed by canonical reading identity and range.
// Media is intentionally added separately from the catalog support.
export const rangeAudioRecordings: RangeAudioRecording[] = []
