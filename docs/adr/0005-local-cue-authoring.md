# Treat Cue Authoring as a local operator workflow

Cue Authoring edits local drafts and exports files; its unlock is a convenience control, not authentication. Keep it lazy and locally scoped, prefer excluding it from ordinary production delivery when practical, and require real server authorization before any future privileged remote write.
