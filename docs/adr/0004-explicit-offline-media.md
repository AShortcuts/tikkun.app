# Keep media downloads explicit

App-shell code and core reader controls may be precached, but recording media is stored offline only after an explicit user-requested Offline Download. Silent media caching would create surprising bandwidth and storage costs, especially as recording coverage grows.
